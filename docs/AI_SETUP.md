# AI Feature Setup for DeBugBuddy

DeBugBuddy supports optional AI-powered explanations using OpenAI (ChatGPT) or Anthropic (Claude). This enhances pattern-based explanations with more context-aware insights.

## Prerequisites

- Install the AI extras (includes anthropic SDK if needed):